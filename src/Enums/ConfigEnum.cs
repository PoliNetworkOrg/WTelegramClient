using System;
using System.Collections.Generic;
using System.Text;

namespace WTelegram.Enums
{
    public enum ConfigEnum
    {
        api_id,
        api_hash,
        session_pathname,
        device_model,
        server_address,
        system_version,
        app_version,
        system_lang_code,
        lang_pack,
        lang_code,
        user_id,
        verification_code,
        password,
        session_key,
        phone_number,
        first_name,
        last_name,
        bot_token
    }
}
